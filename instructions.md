Build a web application, here's your instructions.

*Functionalities*
- Only a single button is showen.
- There should be a box above the button, records how many times the button has been pressed by every users.
- Make sure the counting record function normally under multi-user using.

*System Design*
- Introduce any popular framework to get the application done.
- Use a real Postgre database to store the data.